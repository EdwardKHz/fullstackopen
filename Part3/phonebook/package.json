{
  "name": "phonebook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "powershell -Command \"Remove-Item -Recurse -Force dist -ErrorAction Ignore; cd ../../Part2/phonebook; npm run build; Copy-Item dist -Recurse -Force ../../Part3/phonebook/dist\"",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongoose": "^8.18.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10",
    "eslint": "^9.35.0",
    "nodemon": "^3.1.10"
  }
}
